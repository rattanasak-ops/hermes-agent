name: agent-center
version: 0.3.0
description: "Agent Center catalog, mode-aware routing, packet/receipt validation, owner-review training candidates, and bounded real two-model execution through Hermes-owned credentials. Never reads credentials, writes files, opens a terminal, or persists anything."
author: "Hermes Agent"
kind: standalone
provides_tools:
  - agent_center_list_agents
  - agent_center_get_agent
  - agent_center_list_skills
  - agent_center_route
  - agent_center_prepare_training_candidate
  - agent_center_validate
  - agent_center_execute
