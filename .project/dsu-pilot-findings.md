# DSU-P4 · บัญชีข้อบกพร่องของ shortcut ที่ pilot ตรวจพบ (สิ่งส่งมอบจริงของงานนี้)

> งานแม่ = แก้ตัว shortcut `Use Create Design System` · pilot Root Admin เป็นเครื่องมือพิสูจน์ (Goal Lock `DEC-DSU-002`)
> ทุก finding ต้องจบเป็นการแก้ prompt/มาตรฐาน/เครื่องมือของ shortcut — ไม่ใช่แค่แก้หน้างาน
> เริ่ม 2026-07-16 · อัปเดตทุกครั้งที่ pilot เจอของจริง

| # | ข้อบกพร่องของ shortcut ที่เจอจริง | หลักฐานจากเคส Root Admin | ทางแก้ที่จะใส่กลับเข้า shortcut | สถานะ |
|---|---|---|---|---|
| F-01 | สำเนาชุดมาตรฐานในโปรเจกต์เก่าค้าง (v2/77 ข้อ ไม่มี ds-gate) โดยไม่มีขั้นบังคับตรวจรุ่น — version drift แบบเดียวกับที่ DSU เพิ่งแก้ที่ศูนย์กลาง เกิดซ้ำที่ปลายทาง | พบสำเนาเก่าใน synerry-engine (เช็กลิสต์หัวไฟล์ = v2) ทั้งที่ศูนย์กลางเป็น v3.1 · แก้แล้วด้วย commit `c669a7a4` | เพิ่มขั้นบังคับใน prompt ก้าว 0: เทียบเนื้อสำเนาทั้งชุดกับชุดกลาง (diff/แฮช ไม่ใช่แค่เลขรุ่น) · ไม่ตรง = เสนออัปเดตเป็นงานชิ้นแรกให้เจ้าของอนุมัติ | **ปิดครบ: คลัง `e133f00` + payload ตรงคลัง 100% (แฮชตรง · PR #58 merged `af3aa41db`)** |
| F-02 | prompt สวมบทให้ AI เป็น "Senior Design System Engineer ของโปรเจกต์" เต็มตัว — กลืนเป้าหมายแม่ของคนที่เรียกใช้ ทำ AI ลืมว่ากำลังทดสอบ/แก้เครื่องมือ (เหตุการณ์จริง: AI หลุดเป้ารอบที่ 20 ในแชทนี้) | ข้อความรายงาน 3 รอบล่าสุดของ AI ไม่พูดถึงเป้าหมายแก้ shortcut เลย · ไปถามเจ้าของเรื่องสีแบรนด์ราวกับเจ้าของเป็นลูกค้าดีไซน์ | เพิ่มกติกาในหัว prompt: จด `caller_goal` คำต่อคำลง DesignSystem.md + บังคับบรรทัดประกาศสถานีทุกข้อความ + โหมด pilot (ผลลัพธ์ = รายงาน findings ไม่ใช่แค่ DS ของโปรเจกต์) | **ปิดครบ: คลัง `e133f00` + payload ตรงคลัง 100% (แฮชตรง · PR #58 merged `af3aa41db`)** |
| F-03 | ด่านสี (PHASE 3) บังคับถามเจ้าของ "4 สี + น้ำหนัก" เสมอ แม้โปรเจกต์มีชุดสีใช้งานจริงที่ผ่านการใช้มาแล้ว — สร้างแรงเสียดทานกับเจ้าของโดยไม่จำเป็น | Root Admin มีชุดสีจริงใน ds-tokens.css อยู่แล้ว · AI ต้องถามเจ้าของซ้ำ 2 รอบ เจ้าของไม่ตอบ (สัญญาณว่าคำถามผิดจังหวะ/ผิดคน) | เพิ่มทางลัดใน PHASE 3: จำแนกสีเดิม 3 แบบ (อนุมัติแล้ว/ใช้อยู่ไม่มีหลักฐาน/ไม่มี) → เสนอ "ยืนยันชุดเดิม" โดยเจ้าของยืนยันเสมอ · ถามเต็ม 4 สีเฉพาะไม่มีสีเดิม + แก้บทนำให้ตรงกัน | **ปิดครบ: คลัง `e133f00` + payload ตรงคลัง 100% (แฮชตรง · PR #58 merged `af3aa41db`)** |
| F-04 | relay-call ตีผลเป็น crash ทั้งที่ coder ทำงานสำเร็จและ commit แล้ว (เช็กแค่ไฟล์ค้าง ไม่เช็ก HEAD เลื่อน) — ทำให้สายพานที่ shortcut พึ่งพารายงานผลหลอก | เจอซ้ำ 2 เคสจริง: DSU-P4-I1 (Codex commit `c669a7a4`) และ DSU-P4-I2 (Codex commit `d51d07846`) — relay คืน crash ทั้งคู่ | แก้ตัวจำแนกผลใน relay-call: เก็บ HEAD ก่อนเรียก · HEAD เลื่อน = งานเขียนสำเร็จ (ปักธงงานแยก: task chip "แก้ relay-call ตีผลผิดเมื่อ coder commit เอง") | ปักธงแยกแล้ว |
| F-05 | เครื่องมือของ shortcut (ds-gate/ds-adopt) รันตรงไม่ได้บนเครื่องที่เปิดด่าน New Chat Gate — hook บล็อก python ทุกกรณีแม้เป็นการตรวจอ่านอย่างเดียว ตัวควบคุมต้องอ้อมผ่าน Codex ทุกครั้ง (ช้า+เปลืองรอบ) | ds-gate --layer all (อ่านอย่างเดียว) ถูกบล็อก 3 รูปแบบการเรียกในแชทนี้ · ต้องส่งใบงานให้ Codex รันแทน | เสนอเจ้าของเพิ่ม ds-gate เข้าบัญชีคำสั่ง lifecycle ที่ hook อนุญาต (แนะนำ — เป็นตัวตรวจอ่านอย่างเดียว) หรือคงให้วิ่งผ่าน relay | รอเจ้าของเคาะ |

## เหตุการณ์หลงเป้ารอบที่ 20 (บันทึกไว้กันซ้ำ)
- วันที่ 2026-07-16 · เจ้าของโกรธ ("fuck you เอาอีกแล้ว ทำไมลืมเป้าหมาย")
- ตัวการที่สอบสวนพบ 3 ตัว: (1) F-02 บทใน prompt กลืนเป้าหมายแม่ (2) บันทึก Goal Lock เดิมเขียนให้ pilot ดูเป็นตัวงาน — แก้แล้วใน DEC-DSU-002 (3) แรงกดดันจาก stop-hook ที่บังคับเขียนใหม่หลายรอบ ทำให้แต่ละรอบโฟกัสรูปแบบจนหลุดกรอบเป้าหมาย
- มาตรการถาวรที่ทำแล้ว: DEC-DSU-002 "เป้าหมายแม่" บรรทัดแรก + บังคับบรรทัดประกาศสถานีทุกข้อความ + กฎ F-02 เข้า prompt v3.1 + ไฟล์บัญชี findings นี้เป็นสิ่งส่งมอบหลัก
